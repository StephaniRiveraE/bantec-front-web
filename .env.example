# Ejemplo de .env para despliegue
# Reemplaza con la URL base real de tu backend (sin slash final), por ejemplo:
REACT_APP_API_BASE_URL=https://mi-backend-en-cloud.example.com

# Nota: en CI/CD o Netlify/Vercel/Cloud Run define esta variable en la configuración del despliegue.
