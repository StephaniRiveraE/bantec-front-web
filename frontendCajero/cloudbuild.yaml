steps:
# Paso 1: Construir la imagen Docker
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/cajero-frontend', '.']

# Paso 2: Empujar la imagen al registro
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/cajero-frontend']

# Paso 3: Desplegar en Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
    - 'run'
    - 'deploy'
    - 'cajero-frontend'
    - '--image'
    - 'gcr.io/$PROJECT_ID/cajero-frontend'
    - '--region'
    - 'us-central1'
    - '--port'
    - '80'
    - '--allow-unauthenticated'
    - '--platform'
    - 'managed'
